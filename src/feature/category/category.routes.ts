const baseUrl : string  ="/category/"
export const categoryRoutes = {
    ADD : `${baseUrl}`,
    GET_BY_ID : `${baseUrl}:id`,
    UPDATE :  `${baseUrl}:id`,
    DELETE :  `${baseUrl}:id`,
    LIST : `${baseUrl}list`,
}